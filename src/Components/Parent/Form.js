import React from 'react'
import FormComp from '../Child/FormComp';

const Form = () => {
  return (
    <section className='form-section' id = "form">
        <FormComp/>
    </section>
  )
}

export default Form
